{
  "name": "mpp-nearintents",
  "version": "0.0.0",
  "private": true,
  "description": "Reference implementation of the `nearintents` payment method for MPP (Machine Payments Protocol) — cross-chain HTTP 402 payments settled by NEAR Intents. Extends mppx.",
  "type": "module",
  "sideEffects": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IkerAlus/mpp-nearintents.git"
  },
  "homepage": "https://github.com/IkerAlus/mpp-nearintents#readme",
  "bugs": {
    "url": "https://github.com/IkerAlus/mpp-nearintents/issues"
  },
  "keywords": [
    "mpp",
    "machine-payments",
    "http-402",
    "payments",
    "near",
    "near-intents",
    "cross-chain",
    "typescript"
  ],
  "files": [
    "dist",
    "src",
    "!src/**/*.test.ts"
  ],
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./client": {
      "types": "./dist/client/index.d.ts",
      "default": "./dist/client/index.js"
    },
    "./server": {
      "types": "./dist/server/index.d.ts",
      "default": "./dist/server/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "changeset": "changeset",
    "check": "pnpm check:types && pnpm lint && pnpm test",
    "check:types": "tsc --noEmit",
    "example:client": "tsx --env-file-if-exists=.env examples/client.ts",
    "example:server": "tsx --env-file-if-exists=.env examples/server.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "prepack": "pnpm build",
    "test": "vitest run"
  },
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "peerDependencies": {
    "mppx": "0.8.5",
    "viem": "^2.0.0"
  },
  "peerDependenciesMeta": {
    "viem": {
      "optional": true
    }
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@changesets/cli": "^2.31.0",
    "@types/node": "^26.1.0",
    "mppx": "0.8.5",
    "tsx": "^4.23.0",
    "typescript": "~6.0.3",
    "viem": "^2.54.3",
    "vitest": "^4.1.9"
  }
}
